#ifndef __STRING_CONVERSION
#define __STRING_CONVERSION

#include <string>

size_t str_to_uint32_t(const std::string &m_str);
size_t str_to_size_t(const std::string &m_str);

#endif // __STRING_CONVERSION